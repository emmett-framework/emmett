language: python
dist: xenial

cache:
  directories:
    - $HOME/.pip-cache/

python:
  - "3.7"
  - "3.8"

install:
  - pip install tox>=1.8

script:
  - tox -e $(echo py$TRAVIS_PYTHON_VERSION | tr -d . | tr -d "\-dev" | sed -e 's/pypypy/pypy/')

notifications:
  email: false
